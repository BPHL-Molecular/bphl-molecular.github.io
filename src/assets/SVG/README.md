# Florida outline
florida.svg is copied from the supplied FLMap-outline-green.svg.
The existing SVG folder uses uppercase casing; preserve this casing on Linux.
Update src/config/assets.js if replacing it. Filled paths and holes are supported.
The file is sampled into a flat point cloud; there is no extruded map.
