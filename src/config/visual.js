export const VISUAL_CONFIG = {
  particleCount: 38000,
  reducedParticleCount: 14000,
  pointSize: 0.018,
  // Scroll distances in viewport heights: most of each chapter is now morphing.
  heroScroll: 1.8,
  heroMorphFraction: 0.94,
  chapterScroll: 0.9,
  chapterMorphScroll: 0.65,
  autoRotateSpeed: 0.16,
  // Manual drag cap while the reads/map are facing the camera, in degrees.
  maxReadableDragDeg: 70,
  maxPixelRatio: 1.7,
  // Organic blob, DNA, bacterium, sequence streams, network, Florida.
  morphStops: [0, 0.2, 0.4, 0.6, 0.8, 1],
}
